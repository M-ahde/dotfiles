# 1. استيراد الألوان الديناميكية
source = ~/.cache/wal/colors-hyprland.conf

# BACKGROUND
background {
    monitor =
    path = ~/.config/hypr/hyprlock.png
    # جعل الخلفية تتوافق مع الألوان إذا لم توجد صورة
    color = $background
    
    blur_passes = 1 # تفعيل بلور خفيف يعطي شكلاً زجاجياً
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}



# GENERAL
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = true
}

# Time-Hour (تم ربطه باللون البرتقالي color1)
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I")</span>"
    color = $color1
    font_size = 125
    font_family = StretchPro
    position = -80, 190
    halign = center
    valign = center
}

# Time-Minute (تم ربطه باللون الذهبي color5)
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%M")</span>"
    color = $color5
    font_size = 125
    font_family = StretchPro
    position = 0, 70
    halign = center
    valign = center
}

# Day-Month-Date
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%d %B, %a.")"
    color = $foreground
    font_size = 22
    font_family = Suisse Int'l Mono
    position = 20, -8
    halign = center
    valign = center
}

# USER
label {
    monitor =
    text =   $USER
    color = $color4
    font_size = 20
    font_family = SF Pro Display Bold
    position = 0, -220
    halign = center
    valign = center
}

# INPUT FIELD (تعديل الألوان لتناسب Wal)
input-field {
    monitor =
    size = 300, 60
    outline_thickness = 2
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = $color1
    inner_color = rgba(0, 0, 0, 0.5) # خلفية سوداء شفافة
    font_color = $foreground
    fade_on_empty = false
    font_family = SF Pro Display Bold
    placeholder_text = <i><span foreground="##fffffff99">🔒 Enter Pass</span></i>
    hide_input = false
    position = 0, -290
    halign = center
    valign = center
}

# CURRENT SONG
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/songdetail.sh)"
    color = $color6
    font_size = 16
    font_family = JetBrains Mono Nerd
    position = 0, 40
    halign = center
    valign = bottom
}
animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    
    # جرب هذا السطر تحديداً
    animation = layersIn, 1, 5, myBezier, slide top
    animation = layersOut, 1, 5, myBezier, slide top
}
